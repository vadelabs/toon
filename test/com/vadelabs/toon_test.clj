(ns com.vadelabs.toon-test
  (:require [clojure.test :refer [deftest is testing]]
            [com.vadelabs.toon :as sut])) ; system under test

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
